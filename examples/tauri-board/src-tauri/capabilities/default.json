{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the example window: core defaults plus event listen, so the webview can receive the `nxf-change` re-render cue. The app-defined `next`/`blocked` commands are not gated by the ACL.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
